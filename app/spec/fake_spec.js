define(function() {
    describe('A test', function() {  
        beforeEach(function() {});
        afterEach(function() {});
        it('loads jasmine', function() {
            expect(true).toEqual(true);
        }); 
    });
});